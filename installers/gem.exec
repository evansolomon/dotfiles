#!/usr/bin/env bash

for gem in "capistrano" "railsless-deploy" "bundler" "scatter_deploy"; do
  gem list | ack $gem >/dev/null || {
    echo "Installing $gem from Rubygems"
    gem install $gem >/dev/null 2>&1
  }
done
