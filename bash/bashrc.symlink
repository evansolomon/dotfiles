# I just use bash for testing

[[ -s "$HOME/.nvm/nvm.sh" ]]      && . "$HOME/.nvm/nvm.sh" && nvm use default >/dev/null
source impromptu

# added by travis gem
[ -f "${HOME}/.travis/travis.sh" ] && source "${HOME}/.travis/travis.sh"
