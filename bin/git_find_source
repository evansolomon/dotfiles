#!/usr/bin/env bash

git bisect start
git bisect bad
git bisect good $(git log --pretty=format:%H | tail -1) # Start from the first commit
git bisect run $DOTFILES/bin/regex-does-not-exist $1
git bisect reset
